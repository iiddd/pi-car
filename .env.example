# Pi-Car Environment Configuration
# Copy this file to .env and update with your Pi's credentials
# NEVER commit the .env file to version control!

# Your Raspberry Pi SSH connection
PI_HOST=user@192.168.x.x

# Deployment path on Pi (optional, defaults to /home/user/IdeaProjects/pi-car)
# PI_PATH=/path/to/deployment

# To use these variables, source them before running deploy scripts:
#   source .env && ./scripts/deploy.sh

